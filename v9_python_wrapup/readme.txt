# vim show white spaces
- use commands: set list and set nolist
- to insert a literal tab character use CTRL+V to escape it 
# notepad++ do not replace tab with spaces
- settings -> preferences -> tab settings -> python -> uncheck 'replace by space'
 
